# Simple Java Company Manager

Using Java, this is intended to be a simple program for managing employees for a business.

## How To Use

Import the `Employee` and `Company` java classes into your code editor of choice.